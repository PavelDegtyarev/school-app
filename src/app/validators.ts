import {FormGroup} from "@angular/forms";

export class MyValidators{

  // static findMaxAmount() {
  //   return (form:FormGroup)=>{
  //     let min= form.get('from');
  //     let max= form.get('to');
  //     return min.value && max.value && +max.value < +min.value ? {error:'min malue'}:null
  //   }
  // }
}
